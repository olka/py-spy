import {terser} from "rollup-plugin-terser";
import {default as config} from "./rollup.config.js"

// Same basic rollup config as the default, just running terser to minify
config.output.file = "assets/bundle.min.js"
config.output.sourcemap = false;
config.plugins.push(terser({output: {preamble: config.output.banner}}));

export default config;
